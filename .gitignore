node_modules/
.next/
.next-staging/
.next-old/
.env
.env.local
.env.production
.DS_Store
nul

.vercel
.env*.local

# Local SQLite dev database — never commit real user data or password hashes.
data/*.sqlite
data/*.sqlite-shm
data/*.sqlite-wal

# Python bytecode from tooling scripts
__pycache__/

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Playwright e2e artifacts
test-results/
playwright-report/

# Uploaded repair files (QC/diagnosis reports) — served from disk, not in git
.uploads/
