# Catalog photo pack

Fallback hero + detail JPEGs for Partner API and Operations when a SKU has no staff upload.

- Sources are Wikimedia Commons product photos (see `lib/catalog-photos.ts`).
- Regenerated with `npx tsx scripts/download-catalog-photos.ts`.
- Staff uploads on the product form always win.
