# Pagination rollout

Standardize large operational lists on the Sales-style pagination contract: 10 rows by default, 10/25/50 page-size choices, Previous/numbered/Next navigation, visible record range, mobile-safe controls, and page reset/clamping when list context changes.
